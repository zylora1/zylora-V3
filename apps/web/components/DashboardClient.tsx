'use client';
import {useEffect,useState} from 'react';import Link from 'next/link';import {api,SessionUser,Site} from '../lib/api';
type Lead={id:number;name:string;email:string;source:string;created_at:string;whatsapp_status:'PENDING'|'SENDING'|'SENT'|'FAILED'|'SKIPPED'};
const whatsappLabel=(status:Lead['whatsapp_status'])=>({PENDING:'WhatsApp pending',SENDING:'WhatsApp sending',SENT:'WhatsApp sent',FAILED:'WhatsApp failed',SKIPPED:'WhatsApp not configured'}[status]||'WhatsApp not configured');
function greeting(){const h=new Date().getHours();return h<12?'Good morning.':h<17?'Good afternoon.':'Good evening.'}
export default function DashboardClient(){const[user,setUser]=useState<SessionUser|null>(null);const[sites,setSites]=useState<Site[]>([]);const[leads,setLeads]=useState<Lead[]>([]);const[error,setError]=useState('');useEffect(()=>{Promise.all([api<SessionUser>('/auth/me'),api<Site[]>('/sites'),api<Lead[]>('/leads')]).then(([u,s,l])=>{setUser(u);setSites(s);setLeads(l)}).catch((e:any)=>{if(e.status===401)location.href='/login';else setError(e.message)})},[]);return <><div className="topbar"><div className="pills"><span className="pill">{user?.plan||'Loading'}</span><span className="pill">AI {user?.ai_credits??'—'}</span><span className="pill">Leads {user?.lead_credits??'—'}</span></div><Link className="primaryAction" href="/templates">Create website</Link></div><div className="dashhead"><div><p className="eyebrow darkEye">Workspace</p><h1>{greeting()}</h1></div><span className="muted">{user?.email}</span></div>{error&&<div className="errorPanel">{error}</div>}<section className="metricrow"><div className="metric"><span>Websites</span><b>{sites.length}</b><small>{sites.filter(s=>s.state==='LIVE').length} live</small></div><div className="metric"><span>Leads</span><b>{leads.length}</b><small>Across all sites</small></div><div className="metric"><span>AI credits</span><b>{user?.ai_credits??0}</b><small>Current allowance</small></div></section><section className="dashgrid" id="sites"><div className="panel"><div className="panelTitle"><h3>Your websites</h3><Link href="/templates">Browse templates</Link></div><div className="siteRows">{sites.length?sites.map(s=><Link className="siteRow" key={s.id} href={`/editor/${s.id}`}><div><strong>{s.name}</strong><span>{s.slug} · {s.origin}</span></div><span className={`status ${s.state.toLowerCase()}`}>{s.state}</span></Link>):<div className="emptyState">No sites yet. Choose a template to create your first site.</div>}</div></div><div className="panel" id="leads"><div className="panelTitle"><h3>Recent leads</h3><span>{leads.length}</span></div><div className="activity">{leads.slice(0,6).map(l=><div key={l.id}><span><b>{l.name}</b><small>{l.email}</small><small>{whatsappLabel(l.whatsapp_status)}</small></span><em>{l.source}</em></div>)}{!leads.length&&<div className="emptyState">Leads captured from forms and chatbot will appear here.</div>}</div></div></section></>}
