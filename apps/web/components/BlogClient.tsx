'use client';import {useEffect,useState} from 'react';import Link from 'next/link';import {api} from '../lib/api';export default function BlogClient(){const[posts,setPosts]=useState<any[]>([]);useEffect(()=>{api<any[]>('/blog/public').then(setPosts).catch(()=>setPosts([]))},[]);return <section className="blogGrid">{posts.map(p=><article className="blogCard" key={p.id}>{p.featured_image&&<img src={p.featured_image} alt=""/>}<small>Resources</small><h2>{p.title}</h2><p>{p.excerpt}</p><Link href={`/blog/${p.slug}`}>Read article →</Link></article>)}{!posts.length&&<article className="blogCard"><small>Zylora Journal</small><h2>Publishing is ready.</h2><p>Published posts from SUPER_ADMIN will appear here.</p></article>}</section>}
