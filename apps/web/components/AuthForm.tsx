'use client';
import Script from 'next/script';import {useCallback,useState} from 'react';import Turnstile from './Turnstile';import {api} from '../lib/api';
declare global {interface Window{google?:any}}
export default function AuthForm({mode}:{mode:'login'|'signup'}){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[token,setToken]=useState('');const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);const turn=useCallback((t:string)=>setToken(t),[]);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setMsg('');try{await api(`/auth/${mode}`,{method:'POST',body:JSON.stringify({email,password,turnstile_token:token||null})});location.href='/dashboard'}catch(e:any){setMsg(e.message)}finally{setBusy(false)}}async function google(credential:string){setBusy(true);try{await api('/auth/google',{method:'POST',body:JSON.stringify({id_token:credential,turnstile_token:token||null})});location.href='/dashboard'}catch(e:any){setMsg(e.message)}finally{setBusy(false)}}function googleReady(){if(!window.google)return;window.google.accounts.id.initialize({client_id:process.env.NEXT_PUBLIC_GOOGLE_CLIENT_ID,callback:(r:any)=>google(r.credential)});}
return <main className="auth"><form onSubmit={submit}><img src="/zylora-logo.png" alt="Zylora"/><h1>{mode==='login'?'Welcome back':'Create your account'}</h1><p className="mutedCopy">{mode==='login'?'Manage sites, leads and bookings.':'Launch your first site in minutes.'}</p><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email" required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete={mode==='login'?'current-password':'new-password'} minLength={8} required/></label><Turnstile onToken={turn}/><button disabled={busy}>{busy?'Please wait…':mode==='login'?'Sign in':'Create account'}</button><Script src="https://accounts.google.com/gsi/client" strategy="afterInteractive" onLoad={googleReady} onError={()=>setMsg('Google Sign-In failed to load.')}/><button type="button" className="google" onClick={()=>window.google?.accounts.id.prompt()} disabled={busy}>Continue with Google</button>{msg&&<p className="formError" role="alert">{msg}</p>}{mode==='login'&&<a href="/forgot-password">Forgot password?</a>}<a href={mode==='login'?'/signup':'/login'}>{mode==='login'?'Need an account? Sign up':'Already have an account? Sign in'}</a></form></main>}
