# Zylora Template Catalogue

This directory contains 1,008 standalone TSX source implementations. They intentionally do not import a shared visual `GenericTemplate` component. `manifest.json` is used by the catalogue loader. Production visual review is still required before enabling any individual template for customers.
