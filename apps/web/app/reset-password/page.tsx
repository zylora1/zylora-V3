'use client';import {useState} from 'react';import {api} from '../../lib/api';export default function Page(){const[password,setPassword]=useState('');const[msg,setMsg]=useState('');return <main className="auth"><form onSubmit={async e=>{e.preventDefault();const token=new URLSearchParams(location.search).get('token')||'';try{await api('/auth/password-reset/confirm',{method:'POST',body:JSON.stringify({token,new_password:password})});setMsg('Password updated. You can sign in now.')}catch(e:any){setMsg(e.message)}}}><img src="/zylora-logo.png" alt="Zylora"/><h1>Choose a new password</h1><label>New password<input type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required/></label><button>Update password</button>{msg&&<p>{msg}</p>}</form></main>}
