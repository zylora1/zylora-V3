import type {Metadata} from 'next';import AppShell from '../../components/AppShell';import DashboardClient from '../../components/DashboardClient';export const metadata:Metadata={title:'Dashboard | Zylora',description:'Manage websites, leads, bookings and publishing in Zylora.',robots:{index:false,follow:false}};export default function Page(){return <AppShell><DashboardClient/></AppShell>}
