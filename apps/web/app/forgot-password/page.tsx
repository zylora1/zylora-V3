'use client';import {useState} from 'react';import {api} from '../../lib/api';export default function Page(){const[email,setEmail]=useState('');const[msg,setMsg]=useState('');return <main className="auth"><form onSubmit={async e=>{e.preventDefault();await api('/auth/password-reset/request',{method:'POST',body:JSON.stringify({email})});setMsg('If that account exists, a reset email has been sent.')}}><img src="/zylora-logo.png" alt="Zylora"/><h1>Reset password</h1><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><button>Send reset link</button>{msg&&<p>{msg}</p>}</form></main>}
