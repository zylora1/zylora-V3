import type {MetadataRoute} from 'next';import {API} from '../lib/api';
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_SITE_URL||'http://localhost:3000';const staticPaths=['','/templates','/blog','/managed','/privacy','/terms','/refunds'];let posts:any[]=[];try{const r=await fetch(`${API}/blog/public`,{next:{revalidate:3600}});if(r.ok)posts=await r.json()}catch{}return [...staticPaths.map(path=>({url:`${base}${path}`,lastModified:new Date(),changeFrequency:path===''?'weekly':'monthly' as any,priority:path===''?1:.7})),...posts.map(p=>({url:`${base}/blog/${p.slug}`,lastModified:new Date(),changeFrequency:'monthly' as const,priority:.6}))]}
