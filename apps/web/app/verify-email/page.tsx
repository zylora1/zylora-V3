'use client';import {useEffect,useState} from 'react';import {api} from '../../lib/api';export default function Page(){const[msg,setMsg]=useState('Verifying…');useEffect(()=>{const token=new URLSearchParams(location.search).get('token')||'';api('/auth/email-verification/confirm',{method:'POST',body:JSON.stringify({token})}).then(()=>setMsg('Email verified.')).catch((e:any)=>setMsg(e.message))},[]);return <main className="legal"><h1>Email verification</h1><p>{msg}</p><a className="primaryAction" href="/dashboard">Continue</a></main>}
