# Pan and zoom evidence

The final interaction suite verified in all three engines:

- middle-mouse drag changes canvas scroll offset only;
- Space + left-drag changes canvas scroll offset only;
- selected node inline geometry remains unchanged during both pan gestures;
- zoom controls and canvas Fit remained available;
- all eight viewport widths rendered without application-level horizontal overflow.
