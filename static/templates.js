(async()=>{const count=document.getElementById('templateCount'),catalogue=document.getElementById('catalogue');try{const r=await fetch('/api/templates',{headers:{Accept:'application/json'}}),j=await r.json(),items=Array.isArray(j.items)?j.items:[];if(count)count.textContent=String(items.length);if(!catalogue)return;if(!items.length)return;catalogue.innerHTML=`<div class="catalogue-grid">${items.map(t=>`<article class="catalogue-card"><a class="catalogue-image" href="/template-preview/${encodeURIComponent(t.slug)}" target="_blank" rel="noopener"><img src="${t.preview}" alt="Rendered preview of ${escapeHtml(t.name)}" loading="lazy"></a><div class="catalogue-meta"><div><span>${escapeHtml(t.category||t.industry||'Template')}</span><span>${Number(t.pages||1)} page${Number(t.pages||1)===1?'':'s'}</span></div><h2>${escapeHtml(t.name)}</h2><p>${escapeHtml(t.description||t.art_direction||t.style||'Independently art-directed website')}</p><div class="catalogue-actions"><a href="/template-preview/${encodeURIComponent(t.slug)}" target="_blank" rel="noopener">Preview</a><a class="cta" href="/signup?template=${encodeURIComponent(t.slug)}">Use template</a></div></div></article>`).join('')}</div>`}catch{if(count)count.textContent='—'}})();function escapeHtml(v){return String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
